# ── CipherSchools QA Assignment — Environment Config ──────────────────────────
# Copy this file to .env and fill in your values before running scripts.
# NEVER commit the actual .env file — it is in .gitignore.

# App under test
BASE_URL=https://with-bugs.practicesoftwaretesting.com

# Existing test account (used for login-only flows if needed)
TEST_EMAIL=customer@practicesoftwaretesting.com
TEST_PASSWORD=welcome01

# Search keyword for E2E flow
SEARCH_KEYWORD=pliers

# k6 environment tag (staging | production)
K6_ENV=staging
